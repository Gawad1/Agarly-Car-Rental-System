import type { Parser } from "../types";
import { XmlNode } from "@rgrove/parse-xml";
export declare class XMLParser implements Parser {
    parse(res: Response): Promise<any>;
    clean(xml: string): string;
    jsify(node: XmlNode | XmlNode[]): any;
}
